<van-popup
  show="{{ visible }}"
  position="top"
  @close="handleClose"
  overlay="{{false}}"
  close-icon-position="bottom-right"
  custom-style="position: static;"
>
  <view class="h-full w-full bg-neutral-50 pt-2 max-h-screen border-b border-dashed border-b-slate-400">
    <!-- Chart Content Goes Here -->
    <view wx:if="{{ visible }}" class="w-full h-52 px-4 pl-0 block box-border">
      <qiun-data-charts
        reshow="{{ visible && chartReady }}"
        opts-watch="{{ !chartReady }}"
        type="line"
        animation="{{ false }}"
        chart-data="{{chartData}}"
        opts="{{ chartOptions }}"
        ontap
        ontouch
      />
    </view>

    <!-- Footer -->
    <view class="flex items-center px-4 pb-4">
      <view class="flex-1 text-xs text-gray-400 text-center">点击图例可切换</view>
      <view class="flex justify-end items-center space-x-6">
        <view class="i-ep-close text-2xl" @tap="handleClose" />
      </view>
    </view>
  </view>
</van-popup>
