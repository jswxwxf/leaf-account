<view class="custom-class fixed bottom-4 right-4 z-9" hidden="{{ hidden }}">
  <action-button actions="{{ actions }}" @select="handleActionSelect">
    <van-button type="primary" @click.catch="handleMainClick">
      <van-icon name="records" class="mr-1" />
      记一笔
    </van-button>
  </action-button>
</view>
