<wxs src="../../utils/utils.wxs" module="utils" />

<view class="p-3">
  <block wx:for="{{ accounts }}" wx:key="_id">
    <account-box account="{{ item }}" @tapped="handleAccountTap" />
  </block>
</view>
