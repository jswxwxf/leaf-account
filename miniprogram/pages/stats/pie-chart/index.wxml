<view class="w-full h-52 px-4 pl-0 block box-border">
  <block wx:if="{{ chartReady }}">
    <qiun-data-charts
      type="pie"
      canvas2d
      animation="{{ false }}"
      chart-data="{{ chartData }}"
      opts="{{ chartOptions }}"
    />
  </block>
</view>
