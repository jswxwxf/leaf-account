<view class="w-full h-52 px-4 block box-border">
  <qiun-data-charts
    type="pie"
    canvas2d
    animation="{{ false }}"
    chart-data="{{ chartData }}"
    opts="{{ chartOptions }}"
    @getIndex="onChartTap"
  />
</view>
