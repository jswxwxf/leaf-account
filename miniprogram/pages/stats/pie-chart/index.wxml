<view wx:if="{{ chartReady }}" class="w-full h-52 px-4 pl-0 block box-border">
  <qiun-data-charts
    type="pie"
    canvas2d
    animation="{{ false }}"
    chart-data="{{ chartData }}"
    opts="{{ chartOptions }}"
  />
</view>
