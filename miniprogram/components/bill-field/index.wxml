<van-field
  label="{{ label }}"
  type="{{ type }}"
  value="{{ value }}"
  placeholder="{{ placeholder }}"
  error="{{ showError ? false : fields[field] === false }}"
  error-message="{{ showError ? errors[field][0].message : '' }}"
  @change="handleChange"
/>
