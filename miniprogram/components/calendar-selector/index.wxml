<van-calendar
  show="{{ visible }}"
  @close="onClose"
  @confirm="onConfirm"
  min-date="{{ minDate }}"
  max-date="{{ maxDate }}"
  default-date="{{ defaultDate }}"
  show-confirm="{{ false }}"
  root-portal
/>
